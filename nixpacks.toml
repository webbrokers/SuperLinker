# Конфигурация сборки Railway/Nixpacks для Python-проекта без pip-зависимостей
providers = ["python"]

[variables]
NIXPACKS_PYTHON_VERSION = "3.11"

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python3 app.py"
