# Пример переменных окружения (скопируйте в .env локально)
# НЕ коммитьте .env с реальными секретами!

PORT=5000
BASE_URL=http://localhost:5000
DATABASE_PATH=./data/superlinker.db
ADMIN_PASSWORD=changeme
RATE_LIMIT_PER_DAY=10
